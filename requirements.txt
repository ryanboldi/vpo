# Pinned to match the vendored veRL.  Newer vLLM (>0.12.0) breaks veRL.
# vllm==0.12.0 itself pins torch==2.9.0; install the CUDA build that matches
# your GPU (e.g. the cu128 wheel for Blackwell / RTX 50-series).
torch==2.9.0
vllm==0.12.0

# Core
numpy
scipy
transformers
datasets
ray
wandb
pyarrow
hydra-core

# Domain-specific
sympy            # eureqa
